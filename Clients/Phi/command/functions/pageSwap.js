const config = require("../../../../settings/config");

async function swapMainPage(client, interaction, i, currentPage) {

    const id = i.customId;

    switch(id) {
        case 'tut':
            currentPage
            ;
        case 'team':
            ;
        case 'proj':
            ;
        case 'clus':
            ;
    }


}

module.exports = {
    swapMainPage,
}